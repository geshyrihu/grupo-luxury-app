export interface userCardDto {
  firstName: string;
  lastName: string;
  birth: string;
  active: string;
  profession: string;
  customerId: string;
  phoneNumber: string;
  photoPath: string;
  email: string;
}
